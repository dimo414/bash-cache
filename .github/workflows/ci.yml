name: CI

on: [push]

jobs:
  Test:
    runs-on: ubuntu-latest
    container: dimo414/ci-bash
    strategy:
      matrix:
        bash-version: [5, 4, 3]
    steps:
      - uses: actions/checkout@v1
      - name: "Using bash-${{ matrix.bash-version }}"
        run: bash-${{ matrix.bash-version }} -c 'bats .'
    
  ShellCheck:
    runs-on: ubuntu-latest
    container: dimo414/ci-bash
    steps:
    - uses: actions/checkout@v1
    - name: ShellCheck
      run: |
        bash -c 'shopt -s globstar; shellcheck **/*.sh'
        bash bash-cache-dynamic-shellcheck.sh
